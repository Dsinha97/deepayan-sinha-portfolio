# Private material — never committed. Certificates, transcripts, MBA coursework,
# the original resume PDF, and the MBA Brain snapshot all live here.
private/

# Design reference material — never committed (DSI-131). Third-party design
# assets and component sources kept locally for reference only. Both spellings
# are listed because the folder on disk carries a typo; renaming it should not
# silently start tracking it.
Referernces/
References/

# Build output
dist/
.astro/
.wrangler/

# Dependencies
node_modules/

# Environment
.env
.env.*
!.env.example

# Local agent state
.claude/settings.local.json
.claude/worktrees/

# OS / editor
Thumbs.db
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
